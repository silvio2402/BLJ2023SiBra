{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "CMake: build",
      "command": "cmake",
      "options": {
        "cwd": "${workspaceFolder}/build"
      },
      "args": [
        "./.."
      ],
    }
  ]
}